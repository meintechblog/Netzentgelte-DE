# TWS Netz Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add `TWS Netz GmbH` to the curated operator registry with a fully verified 2026 modul-3 tariff matrix based on official operator sources.

**Architecture:** Extend the seed registry with one new operator record that matches the current `sourceDocuments` plus `currentTariff` structure. Then update the seed-backed tests so counts and operator-specific assertions reflect the new verified import payload.

**Tech Stack:** JSON seed data, Vitest, seed-backed registry import helpers

---

### Task 1: Add a failing registry assertion for TWS Netz

**Files:**
- Modify: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/modules/operators/registry.test.ts`
- Test: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/modules/operators/registry.test.ts`

**Step 1: Write the failing test**
- Assert that `tws-netz` exists with `reviewStatus: "verified"` and includes at least one `HT` window `17:00-22:00`.

**Step 2: Run test to verify it fails**
- Run: `pnpm -C /Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap exec vitest run src/modules/operators/registry.test.ts`

**Step 3: Write minimal implementation**
- Add the `tws-netz` entry to `operators.seed.json` with `sourceDocuments`, `bands`, and `timeWindows`.

**Step 4: Run test to verify it passes**
- Re-run the same Vitest file.

### Task 2: Update import and published-catalog expectations

**Files:**
- Modify: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/modules/operators/registry-import.test.ts`
- Modify: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/modules/operators/current-catalog.test.ts`

**Step 1: Write the failing test**
- Increase total tariff counts by `3`.
- Increase published verified count by `1`.
- Assert that `tws-netz` is included in the seed-backed published catalog.

**Step 2: Run tests to verify they fail**
- Run: `pnpm -C /Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap exec vitest run src/modules/operators/registry-import.test.ts src/modules/operators/current-catalog.test.ts`

**Step 3: Write minimal implementation**
- No code beyond the seed should be necessary; only keep test expectations aligned with the seed-backed output.

**Step 4: Run tests to verify they pass**
- Re-run the same Vitest files.

### Task 3: Run focused regression

**Files:**
- Verify: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/modules/operators/registry.test.ts`
- Verify: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/modules/operators/registry-import.test.ts`
- Verify: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/modules/operators/current-catalog.test.ts`
- Verify: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/app/api/operators/route.test.ts`
- Verify: `/Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap/src/app/api/tariffs/current/route.test.ts`

**Step 1: Run focused regression**
- Run: `pnpm -C /Users/hulki/projects/netzentgelte-de/.worktrees/bootstrap exec vitest run src/modules/operators/registry.test.ts src/modules/operators/registry-import.test.ts src/modules/operators/current-catalog.test.ts src/app/api/operators/route.test.ts src/app/api/tariffs/current/route.test.ts`

**Step 2: Confirm outcome**
- Expect all tests to pass with `tws-netz` present in the seed-backed output.
